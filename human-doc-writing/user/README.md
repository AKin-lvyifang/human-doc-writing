# 用户写作画像

这里由 `$human-doc-writing` 自动维护。

- `portraits/`：每种文章类型当前使用的正向画像。
- `anti-patterns/`：每种文章类型当前使用的反例画像。
- `history/`：被替换或删除的旧版本。
- `portrait-index.md`：当前画像列表。

不需要手工填写 YAML，也不需要维护多篇样文。

升级 Skill 时必须保留整个 `user/` 目录。
