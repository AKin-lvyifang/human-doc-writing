# 七个 UI Skills 装好以后，先把总入口说清楚

用户在 Codex 里输入 `$`，眼前出现七个以 `Better` 开头的选项。它们看上去都和界面有关，第一次使用的人很难判断从哪里开始。

七个工具各有分工。其中六个负责专项检查，`Better Interface` 负责完整评审，也会协调另外六个专项。列表没有提前说明这层关系，用户才会把七个入口理解成七次独立检查。

日常评审先选择 `Better Interface`，再写清评审对象和边界。想做完整检查就用 `full`，只看主路径问题就用 `quick`。评审默认只读，不会自动修改代码。
